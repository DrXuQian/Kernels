# 边缘AI·惊喜开箱4：端云协同省钱计划

《端云协同省 Token》报告按《边缘AI·惊喜开箱3：视频生成省钱计划》的风格重写后的版本
（系列标题、蓝色导语、目录框、表情包开场、信息图、附：数据来源）。

重新生成：

```bash
cd build && python3 gen.py original.docx ../out.docx
```

`original.docx` 只提供钉钉文档的样式包（styles/numbering/settings），正文由 `gen.py` 里的内容表生成。

## v2：隐私 → 省钱 → 架构

`v2/gen2.py` 按「隐私、省钱、架构」三章重排，口吻更口语化，分场景完整表移到附一，
并引用 NVIDIA 博客中的 Switchyard 成本/完成率图。

```bash
cd v2 && python3 gen2.py original.docx ../out2.docx
```
