diff --git a/CMakeLists.txt b/CMakeLists.txt
index 272f9a38..daab27e8 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -126,7 +126,7 @@ set(CUTLASS_PPU_ARCHS_ENABLED ${CUTLASS_PPU_ARCHS} CACHE STRING "The PPU archite
 # compute_capability convention internally.
 set(CUTLASS_LIBRARY_GENERATOR_ARCHS "")
 foreach(ARCH IN LISTS CUTLASS_PPU_ARCHS_ENABLED)
-  if(ARCH STREQUAL "ppu0010")
+  if(ARCH STREQUAL "ppu001")
     list(APPEND CUTLASS_LIBRARY_GENERATOR_ARCHS "80a")
   elseif(ARCH STREQUAL "ppu0015")
     list(APPEND CUTLASS_LIBRARY_GENERATOR_ARCHS "89")
diff --git a/cmake/PPUToolchain.cmake b/cmake/PPUToolchain.cmake
index 1668e440..bb6d46c8 100644
--- a/cmake/PPUToolchain.cmake
+++ b/cmake/PPUToolchain.cmake
@@ -97,15 +97,15 @@ set(CUTLASS_PPU_HOST_COMPILE   ON CACHE BOOL "PPU build is active (host=g++, dev
 ###############################################################################

 # Parse CUTLASS_PPU_ARCHS into a validated list and derive per-arch
-# -arch=ppu_XX flags for hgcc.  Defaults to both ppu0010 and ppu0015.
-set(_PPU_ALLOWED_ARCHS ppu0010 ppu0015)
+# -arch=<arch> flags for hgcc (arch name passed straight through). Defaults to ppu001.
+set(_PPU_ALLOWED_ARCHS ppu001 ppu0015 all)

 if (DEFINED CUTLASS_PPU_ARCHS AND NOT "${CUTLASS_PPU_ARCHS}" STREQUAL "")
   string(REPLACE "," ";" _PPU_ARCHS_LIST "${CUTLASS_PPU_ARCHS}")
   string(REPLACE " " ";" _PPU_ARCHS_LIST "${_PPU_ARCHS_LIST}")
   list(FILTER _PPU_ARCHS_LIST EXCLUDE REGEX "^$")
 else()
-  set(_PPU_ARCHS_LIST "ppu0010;ppu0015")
+  set(_PPU_ARCHS_LIST "ppu001")
 endif()

 foreach(_ARCH IN LISTS _PPU_ARCHS_LIST)
@@ -117,11 +117,7 @@ endforeach()

 set(_PPU_HGCC_ARCH_FLAGS)
 foreach(_ARCH IN LISTS _PPU_ARCHS_LIST)
-  if (_ARCH STREQUAL "ppu0010")
-    list(APPEND _PPU_HGCC_ARCH_FLAGS "-arch=ppu_10")
-  elseif (_ARCH STREQUAL "ppu0015")
-    list(APPEND _PPU_HGCC_ARCH_FLAGS "-arch=ppu_15")
-  endif()
+  list(APPEND _PPU_HGCC_ARCH_FLAGS "-arch=${_ARCH}")
 endforeach()

 list(GET _PPU_ARCHS_LIST 0 _PPU_ARCH_PRIMARY)
@@ -138,7 +134,7 @@ set(CUTLASS_PPU_ARCHS "${_PPU_ARCHS_LIST}" CACHE STRING
 #
 #   --forward-unknown-to-host-compiler : let hgcc forward unknown host flags
 #   --forward-unknown-to-host-linker   : same for link phase
-#   -arch=ppu_10 / ppu_15   : selects the PPU device architecture
+#   -arch=ppu001 / ppu0015  : selects the PPU device architecture
 #   -x hg                   : selects the PPU device language
 #   -DSWITCH_TO_HGGCRT      : enables the hggc runtime layer
 #   -Xcompiler -ftemplate-depth=8192 : deep template hierarchies
